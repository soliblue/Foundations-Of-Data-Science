from Perceptron.perceptron import *;

TrainingSet = [([4, 2, 1], -1), ([-1, 2, -4], 1), ([8, -2, -3], 1),
               ([1, -1, 1], -1), ([2, -2, 5], -1), ([-6, 2, 7], -1)]

perceptron(normalize(TrainingSet));
