# import numpy as np
#
# def powerIteration(matrix, iterations=float('inf')):
#
#     if matrix is not None:
#         matrixRows = len(matrix)
#         matrixColumns = len(matrix[0])
#         unitVector = np.ones(matrixColumns)
#         matrixTranspose = np.transpose(matrix)
#
#     for iteration in range(0,iterations):
#
